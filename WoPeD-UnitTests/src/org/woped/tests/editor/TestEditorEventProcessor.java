package org.woped.tests.editor;

import java.util.Map;

import org.junit.Test;
import org.woped.core.model.CreationMap;

public class TestEditorEventProcessor {

	/*
	 * PowerMockito.spy(new AccountDAO(0));
		PowerMockito.when(mockedDAO.countOverdrawnDaysThisMonth()).thenReturn((long) 10);
	 */
	
	@SuppressWarnings("unused")
	@Test
	public void testPaste(){
		// create mocked elements
		Map<String, CreationMap> pasteElements;
        Map<String, CreationMap> pasteArcs;

		// paste it
		
		// verify positioning
		
		// verify counter in statusbar
	}
}
